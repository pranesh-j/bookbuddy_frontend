/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['bookbuddy-backend-i99j.onrender.com'],
        unoptimized: true
    },
    basePath: '',
}

module.exports = nextConfig 